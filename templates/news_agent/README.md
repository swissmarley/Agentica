# News Scraper Agent

A Streamlit app that configures and runs a news scraping job, then renders results in a reader-friendly UI.

Key features:
- Uses Playwright via `agent_backend.py` to fetch and parse pages
- Presents a card-based dashboard and a reader view
- Shows raw metadata and HTML when needed

Run:
- `streamlit run app.py`
